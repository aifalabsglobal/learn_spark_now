# Clerk — get your key from https://dashboard.clerk.com → API Keys → Publishable key
# This is a Vite app: use VITE_CLERK_PUBLISHABLE_KEY (not NEXT_PUBLIC_*).
# NEVER add CLERK_SECRET_KEY here — that is for server-side only (Node/Next API routes).
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
